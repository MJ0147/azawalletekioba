steps:
  - name: docker
    args:
      - build
      - -f
      - Dockerfile.universal
      - -t
      - ${_IMAGE}
      - .

  - name: docker
    args:
      - push
      - ${_IMAGE}

  - name: doctl
    args:
      - kubernetes
      - cluster
      - kubeconfig
      - save
      - ${_DO_CLUSTER_NAME}

  - name: doctl
    args:
      - kubernetes
      - cluster
      - kubeconfig
      - use
      - ${_DO_CLUSTER_NAME}

  - name: kubectl
    args:
      - rollout
      - restart
      - deployment/${_DO_DEPLOYMENT_NAME}
      - -n
      - ${_DO_NAMESPACE}

images:
  - ${_IMAGE}
