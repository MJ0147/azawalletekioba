apiVersion: v1
kind: Service
metadata:
  name: store
  namespace: ekioba
  labels:
    app: store
    app.kubernetes.io/part-of: ekioba
spec:
  type: ClusterIP
  selector:
    app: store
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
