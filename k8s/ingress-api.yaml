---
# Store API: /api/store/* → store-svc:8001/*
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ekioba-api-store
  namespace: ekioba
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - ekioba.example.com
      secretName: ekioba-tls
  rules:
    - host: ekioba.example.com
      http:
        paths:
          - path: /api/store(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: store-svc
                port:
                  number: 8001

---
# Cargo API: /api/cargo/* → cargo-svc:8002/*
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ekioba-api-cargo
  namespace: ekioba
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - ekioba.example.com
      secretName: ekioba-tls
  rules:
    - host: ekioba.example.com
      http:
        paths:
          - path: /api/cargo(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: cargo-svc
                port:
                  number: 8002

---
# Hotels API: /api/hotels/* → hotels-svc:8003/*
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ekioba-api-hotels
  namespace: ekioba
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - ekioba.example.com
      secretName: ekioba-tls
  rules:
    - host: ekioba.example.com
      http:
        paths:
          - path: /api/hotels(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: hotels-svc
                port:
                  number: 8003

---
# Language Academy API: /api/academy/* → language-academy-svc:8000/*
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ekioba-api-academy
  namespace: ekioba
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - ekioba.example.com
      secretName: ekioba-tls
  rules:
    - host: ekioba.example.com
      http:
        paths:
          - path: /api/academy(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: language-academy-svc
                port:
                  number: 8000
