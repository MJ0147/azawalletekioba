apiVersion: v1
kind: Service
metadata:
  name: hotels-svc
  namespace: ekioba
  labels:
    app: hotels
    app.kubernetes.io/name: hotels
    app.kubernetes.io/part-of: ekioba
spec:
  selector:
    app: hotels
  ports:
    - name: http
      protocol: TCP
      port: 8003
      targetPort: 8003
  type: ClusterIP
